@model List<ArticleCard>

@if (Model.Count > 0)
{
    <ul data-layout="Articles/List"> 
        @foreach (var card in Model)
        {
            <li>
                @await Html.PartialAsync(card.RazorView, card)
            </li>
        }
    </ul>
}